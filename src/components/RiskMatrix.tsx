'use client';
import { useState } from 'react';
const labels=['Sangat rendah','Rendah','Sedang','Tinggi','Sangat tinggi'];
export function RiskMatrix(){const [likelihood,setLikelihood]=useState(3);const [severity,setSeverity]=useState(3);const score=likelihood*severity;const level=score>=17?'Ekstrem':score>=10?'Tinggi':score>=5?'Sedang':'Rendah';return <div className="risk-tool"><div className="tool-fields"><label>Kemungkinan <b>{likelihood} · {labels[likelihood-1]}</b><input type="range" min="1" max="5" value={likelihood} onChange={e=>setLikelihood(Number(e.target.value))}/></label><label>Keparahan <b>{severity} · {labels[severity-1]}</b><input type="range" min="1" max="5" value={severity} onChange={e=>setSeverity(Number(e.target.value))}/></label></div><output className={`risk-output risk-${level.toLowerCase()}`} aria-live="polite"><small>SKOR AWAL</small><strong>{score}</strong><span>{level}</span></output><p>Hasil ini adalah alat penyaringan. Gunakan kriteria risiko organisasi, uraikan skenario secara jelas, dan verifikasi efektivitas pengendalian sebelum mengambil keputusan.</p></div>}
